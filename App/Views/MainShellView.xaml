<UserControl x:Class="Omnos.Desktop.App.Views.MainShellView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="{Binding IsSidebarExpanded, Converter={StaticResource BooleanToWidthConverter}}"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Border Grid.Column="0" Background="CornflowerBlue">
            <StackPanel>
                <Button Command="{Binding ToggleSidebarCommand}" Content="Toggle" Margin="10"/>
                <TextBlock Text="{Binding IsSidebarExpanded, StringFormat='Estado: {0}'}" Foreground="White" Margin="10"/>
            </StackPanel>
        </Border>

        <Border Grid.Column="1" Background="LightGray">
            <TextBlock Text="Área de Conteúdo" HorizontalAlignment="Center" VerticalAlignment="Center" FontSize="24"/>
        </Border>
    </Grid>
</UserControl>