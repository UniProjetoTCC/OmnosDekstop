<UserControl x:Class="Omnos.Desktop.App.Views.ForgotPasswordView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:converters="clr-namespace:Omnos.Desktop.App.Converters"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800"
             Background="#F5F5F5">

    <UserControl.Resources>
        <converters:BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
    </UserControl.Resources>

    <Grid>
        <Border Width="400"
                Background="White"
                CornerRadius="10"
                Padding="30"
                VerticalAlignment="Center"
                HorizontalAlignment="Center">
            <Border.Effect>
                <DropShadowEffect BlurRadius="15" Direction="315" Opacity="0.1" ShadowDepth="5"/>
            </Border.Effect>

            <StackPanel>
                <TextBlock Text="Recuperação de Senha" 
                           FontSize="22" 
                           FontWeight="Bold" 
                           Margin="0,0,0,10"
                           HorizontalAlignment="Center"/>

                <TextBlock Text="Digite seu e-mail para receber um código de recuperação de senha. Você receberá um token por e-mail que será solicitado na próxima tela."
                           TextWrapping="Wrap"
                           Foreground="Gray"
                           TextAlignment="Center"
                           Margin="0,0,0,25"/>

                <TextBlock Text="E-mail" FontWeight="SemiBold"/>
                <TextBox Text="{Binding Email, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource LoginInputStyle}"/>

                <Grid Margin="0,10,0,15">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    
                    <Button Grid.Column="1" 
                            Content="Voltar"
                            Command="{Binding BackToLoginCommand}"
                            Style="{StaticResource DefaultButtonStyle}"
                            Padding="20,10"
                            Margin="0,0,10,0"
                            FontSize="14"
                            FontWeight="SemiBold"/>
                    
                    <Button Grid.Column="2" 
                            Content="Enviar"
                            Command="{Binding SendResetEmailCommand}"
                            Style="{StaticResource DefaultButtonStyle}"
                            Padding="20,10"
                            Margin="10,0,0,0"
                            FontSize="14"
                            FontWeight="SemiBold"/>
                </Grid>

                <TextBlock Text="{Binding ErrorMessage}"
                           Foreground="Red"
                           TextWrapping="Wrap"
                           HorizontalAlignment="Center"
                           Visibility="{Binding Path=ErrorMessage, Converter={StaticResource BooleanToVisibilityConverter}}"/>

                <ProgressBar IsIndeterminate="True" 
                             Height="4" 
                             Margin="0,15,0,0"
                             Visibility="{Binding IsBusy, Converter={StaticResource BooleanToVisibilityConverter}}"/>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
