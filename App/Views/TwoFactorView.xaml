<UserControl x:Class="Omnos.Desktop.App.Views.TwoFactorView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:converters="clr-namespace:Omnos.Desktop.App.Converters"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800"
             Background="#F5F5F5">

    <UserControl.Resources>
        <converters:BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
    </UserControl.Resources>

    <Grid>
        <Border Width="400"
                Background="White"
                CornerRadius="10"
                Padding="30"
                VerticalAlignment="Center"
                HorizontalAlignment="Center">
            <Border.Effect>
                <DropShadowEffect BlurRadius="15" Direction="315" Opacity="0.1" ShadowDepth="5"/>
            </Border.Effect>

            <StackPanel>
                <TextBlock Text="Verificação em Duas Etapas" 
                           FontSize="22" 
                           FontWeight="Bold" 
                           Margin="0,0,0,10"
                           HorizontalAlignment="Center"/>

                <TextBlock Text="Um código foi enviado para o seu dispositivo. Por favor, insira-o abaixo."
                           TextWrapping="Wrap"
                           Foreground="Gray"
                           TextAlignment="Center"
                           Margin="0,0,0,25"/>

                <TextBlock Text="Código de Verificação" FontWeight="SemiBold"/>
                <TextBox Text="{Binding VerificationCode, UpdateSourceTrigger=PropertyChanged}" 
                         Margin="0,5,0,15"
                         Padding="10,8"
                         FontSize="16"
                         TextAlignment="Center"/>

                <Button Content="Verificar" 
                        Command="{Binding VerifyCommand}"
                        Style="{StaticResource DefaultButtonStyle}"
                        Padding="20,10"
                        FontSize="14"
                        FontWeight="SemiBold"
                        Margin="0,10,0,15"/>

                <TextBlock Text="{Binding ErrorMessage}"
                           Foreground="Red"
                           TextWrapping="Wrap"
                           HorizontalAlignment="Center"
                           Visibility="{Binding Path=ErrorMessage, Converter={StaticResource BooleanToVisibilityConverter}}"/>

                <ProgressBar IsIndeterminate="True" 
                             Height="4" 
                             Margin="0,15,0,0"
                             Visibility="{Binding IsBusy, Converter={StaticResource BooleanToVisibilityConverter}}"/>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>