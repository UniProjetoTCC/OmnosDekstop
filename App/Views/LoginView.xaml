<UserControl x:Class="Omnos.Desktop.App.Views.LoginView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:helpers="clr-namespace:Omnos.Desktop.App.Helpers"
             xmlns:converters="clr-namespace:Omnos.Desktop.App.Converters"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800"
             Background="#F5F5F5">

    <UserControl.Resources>
        <converters:BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>

        <Style x:Key="IconTextBoxStyle" TargetType="Grid">
            <Setter Property="Margin" Value="0,5,0,15"/>
        </Style>

        <Style x:Key="IconStyle" TargetType="Path">
            <Setter Property="Fill" Value="#AAAAAA"/>
            <Setter Property="Width" Value="15"/>
            <Setter Property="Height" Value="15"/>
            <Setter Property="Margin" Value="15,0,0,0"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
        </Style>

        <Style x:Key="LoginInputStyle" TargetType="TextBox">
            <Setter Property="Padding" Value="40,10,15,10"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush" Value="#DDDDDD"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border CornerRadius="5" 
                                Background="White"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}">
                            <ScrollViewer x:Name="PART_ContentHost"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="LoginPasswordBoxInputStyle" TargetType="PasswordBox">
            <Setter Property="Padding" Value="40,10,15,10"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush" Value="#DDDDDD"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="PasswordBox">
                        <Border CornerRadius="5" 
                                Background="White"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}">
                            <ScrollViewer x:Name="PART_ContentHost"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Grid>
        <Border Width="350"
                Background="White"
                CornerRadius="10"
                Padding="30"
                VerticalAlignment="Center"
                HorizontalAlignment="Center">
            <Border.Effect>
                <DropShadowEffect BlurRadius="15" Direction="315" Opacity="0.1" ShadowDepth="5"/>
            </Border.Effect>

            <StackPanel>
                <TextBlock Text="Bem-vindo ao Omnos" 
                           FontSize="24" 
                           FontWeight="Bold" 
                           Margin="0,0,0,20"
                           HorizontalAlignment="Center"/>

                <TextBlock Text="Email" FontWeight="SemiBold"/>
                <Grid Style="{StaticResource IconTextBoxStyle}">
                    <Path Data="M0,0 L24,0 L24,24 L0,24z" Fill="Transparent"/>
                    <Path Data="M20,4H4C2.9,4,2,4.9,2,6v12c0,1.1,0.9,2,2,2h16c1.1,0,2-0.9,2-2V6C22,4.9,21.1,4,20,4z M20,8l-8,5l-8-5V6l8,5l8-5V8z" 
                          Style="{StaticResource IconStyle}"/>
                    <TextBox Text="{Binding Email, UpdateSourceTrigger=PropertyChanged}" 
                             Style="{StaticResource LoginInputStyle}"/>
                </Grid>

                <TextBlock Text="Senha" FontWeight="SemiBold"/>
                <Grid Style="{StaticResource IconTextBoxStyle}">
                    <Path Data="M0,0 L24,0 L24,24 L0,24z" Fill="Transparent"/>
                    <Path Data="M18,8h-1V6c0-2.76-2.24-5-5-5S7,3.24,7,6v2H6c-1.1,0-2,0.9-2,2v10c0,1.1,0.9,2,2,2h12c1.1,0,2-0.9,2-2V10C20,8.9,19.1,8,18,8z M9,6c0-1.66,1.34-3,3-3s3,1.34,3,3v2H9V6z"
                          Style="{StaticResource IconStyle}"/>
                    <PasswordBox helpers:PasswordBoxHelper.Attach="True"
                                 helpers:PasswordBoxHelper.Password="{Binding Password, Mode=TwoWay}"
                                 Style="{StaticResource LoginPasswordBoxInputStyle}"/>
                </Grid>

                <Button Content="Entrar" 
                        Command="{Binding LoginCommand}" 
                        Style="{StaticResource DefaultButtonStyle}"
                        Padding="20,10"
                        FontSize="14"
                        FontWeight="SemiBold"
                        Margin="0,10,0,15"/>

                <TextBlock Text="{Binding ErrorMessage}"
                           Foreground="Red"
                           TextWrapping="Wrap"
                           HorizontalAlignment="Center"
                           Visibility="{Binding Path=ErrorMessage, Converter={StaticResource BooleanToVisibilityConverter}}"/>

                <ProgressBar IsIndeterminate="True" 
                             Height="4" 
                             Margin="0,15,0,0"
                             Visibility="{Binding IsBusy, Converter={StaticResource BooleanToVisibilityConverter}}"/>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>