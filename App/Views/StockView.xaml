<UserControl x:Class="Omnos.Desktop.App.Views.StockView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="700" d:DesignWidth="1100"
             Background="#F5F5F5">

    <UserControl.Resources>

        <!-- TextBox "clean", igual ao Login -->
        <Style x:Key="CardTextBoxStyle" TargetType="TextBox">
            <Setter Property="Padding" Value="12"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush" Value="#DDDDDD"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border CornerRadius="5"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}">
                            <ScrollViewer x:Name="PART_ContentHost"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- DataGrid com cabeçalho claro e linhas suaves -->
        <Style TargetType="DataGrid">
            <Setter Property="RowBackground" Value="#FFFFFFFF"/>
            <Setter Property="AlternatingRowBackground" Value="#FFF9F9F9"/>
            <Setter Property="BorderBrush" Value="#EFEFEF"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="HeadersVisibility" Value="Column"/>
            <Setter Property="ColumnHeaderHeight" Value="36"/>
            <Setter Property="GridLinesVisibility" Value="None"/>
        </Style>
        <Style TargetType="DataGridColumnHeader">
            <Setter Property="Background" Value="#FAFAFA"/>
            <Setter Property="Foreground" Value="#444"/>
            <Setter Property="BorderBrush" Value="#E6E6E6"/>
            <Setter Property="BorderThickness" Value="0,0,0,1"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
        </Style>

    </UserControl.Resources>

    <Grid>
        <Border Background="White"
                CornerRadius="10"
                Padding="24"
                Margin="24"
                VerticalAlignment="Stretch"
                HorizontalAlignment="Stretch">
            <Border.Effect>
                <DropShadowEffect BlurRadius="15" Direction="315" Opacity="0.10" ShadowDepth="5"/>
            </Border.Effect>

            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <!-- Titulo -->
                <StackPanel Orientation="Horizontal" Grid.Row="0" Margin="0,0,0,12">
                    <TextBlock Text="Estoque"
                               FontSize="22"
                               FontWeight="Bold"
                               Foreground="#333"/>
                    <TextBlock Text="  &#8226;  gerenciamento e pesquisa"
                               Foreground="#888"
                               Margin="6,3,0,0"/>
                </StackPanel>

                <!-- Filtros / Acoes topo -->
                <Grid Grid.Row="1" Margin="0,0,0,16">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <TextBox Grid.Column="0"
                             Style="{StaticResource CardTextBoxStyle}"
                             Margin="0,0,12,0"
                             Text="{Binding Query, UpdateSourceTrigger=PropertyChanged}"
                             ToolTip="Pesquisar por produto, SKU, localizacao..." />

                    <StackPanel Grid.Column="1"
                                Orientation="Horizontal"
                                HorizontalAlignment="Right">
                        <Button Content="Buscar"
                                Command="{Binding RefreshCmd}"
                                Style="{StaticResource DefaultButtonStyle}"
                                Padding="30,14"
                                FontSize="16"
                                MinWidth="110"
                                Margin="0,0,8,0"/>

                        <Button Content="Limpar"
                                Command="{Binding ClearFilterCmd}"
                                Style="{StaticResource DefaultButtonStyle}"
                                Padding="30,14"
                                FontSize="16"
                                MinWidth="110"
                                Margin="0,0,8,0"/>

                        <Button Content="Novo"
                                Command="{Binding NewCmd}"
                                Style="{StaticResource DefaultButtonStyle}"
                                Padding="30,14"
                                FontSize="16"
                                MinWidth="110"
                                Margin="0,0,8,0"/>

                        <Button Content="Salvar"
                                Command="{Binding SaveCmd}"
                                Style="{StaticResource DefaultButtonStyle}"
                                Padding="30,14"
                                FontSize="16"
                                MinWidth="110"
                                Margin="0,0,8,0"/>

                        <Button Content="Excluir"
                                Command="{Binding DeleteCmd}"
                                Style="{StaticResource DefaultButtonStyle}"
                                Padding="30,14"
                                FontSize="16"
                                MinWidth="110"/>
                    </StackPanel>
                </Grid>

                <!-- Tabela -->
                <DataGrid Grid.Row="2"
                          ItemsSource="{Binding Items}"
                          SelectedItem="{Binding Selected}"
                          AutoGenerateColumns="False"
                          CanUserAddRows="False"
                          IsReadOnly="False"
                          Margin="0,0,0,8">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="ID"
                                            Binding="{Binding Id}"
                                            Width="180"
                                            IsReadOnly="True"/>
                        <DataGridTextColumn Header="Produto"
                                            Binding="{Binding ProductName}"
                                            Width="*"/>
                        <DataGridTextColumn Header="SKU"
                                            Binding="{Binding Sku}"
                                            Width="140"/>
                        <DataGridTextColumn Header="Quantidade"
                                            Binding="{Binding Quantity}"
                                            Width="120"/>
                        <DataGridTextColumn Header="Localizacao"
                                            Binding="{Binding Location}"
                                            Width="180"/>
                        <DataGridTextColumn Header="Validade"
                                            Binding="{Binding ExpiresAt, StringFormat={}{0:dd/MM/yyyy}}"
                                            Width="160"/>
                    </DataGrid.Columns>
                </DataGrid>

                <!-- Rodape -->
                <DockPanel Grid.Row="3">
                    <TextBlock DockPanel.Dock="Left"
                               Text="{Binding Items.Count, StringFormat={}{0} itens}"
                               Foreground="#666"
                               Margin="0,4,12,0"/>
                    <TextBlock DockPanel.Dock="Left"
                               Text="{Binding Selected.ProductName, StringFormat=Selecionado: {0}}"
                               Foreground="#666"
                               Margin="0,4,12,0"/>
                </DockPanel>

            </Grid>
        </Border>
    </Grid>
</UserControl>
